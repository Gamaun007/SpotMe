import SwiftUI

extension Color {
    static let spotPrimary = Color.accentColor
    static let spotBackground = Color(UIColor.systemBackground)
    static let spotSecondaryBackground = Color(UIColor.secondarySystemBackground)
    static let spotText = Color(UIColor.label)
    static let spotSecondaryText = Color(UIColor.secondaryLabel)
}
